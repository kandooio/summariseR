# Declare Variables to pass to the summariseR script
# In this instance, we are passing an Irish Times Article with 2 paragraphs limit on the summary

arg1 <- 'https://www.irishtimes.com/1.3249486'
arg2 <- 2
source('../summariseR.r')
